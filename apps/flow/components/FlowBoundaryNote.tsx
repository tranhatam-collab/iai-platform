export function FlowBoundaryNote() {
  return (
    <section className="border-t border-white/10 px-6 py-16 sm:px-10 lg:px-16">
      <div className="mx-auto max-w-5xl rounded-3xl border border-gold/25 bg-gold/10 p-8">
        <h2 className="text-2xl font-semibold text-white">Boundary</h2>
        <p className="mt-4 text-sm leading-7 text-white/70">
          `flow.iai.one` is the workflow product surface. It is not the main portal and not the
          community destination. Community and public product usage belong in `app.iai.one`.
        </p>
      </div>
    </section>
  )
}
